#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    int a;
    long b;
    char c;
    float d;
    double e;
    
    // Read input values
    scanf("%d %ld %c %f %lf", &a, &b, &c, &d, &e);
    
    // Print each value on a new line
    printf("%d\n", a);
    printf("%ld\n", b);
    printf("%c\n", c);
    printf("%.3f\n", d);       // 3 decimal places for float
    printf("%.9lf\n", e);      // 9 decimal places for double
    
    return 0;
}
